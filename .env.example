# Database (docker compose provides this automatically for dev)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/portfolio"

# Better Auth — generate a secret: openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# Admin credentials used by `npm run db:seed` to create your login
ADMIN_EMAIL=""
ADMIN_PASSWORD=""

# Cloudinary (free tier is fine) — dashboard.cloudinary.com
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY="
CLOUDINARY_API_SECRET=""
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
